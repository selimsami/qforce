# Codecov configuration to make it a bit less noisy
coverage:
  status:
    patch: true
    project:
      default:
        threshold: 80%
comment:
  layout: "header"
  require_changes: false
  branches: null
  behavior: default
  flags: null
  paths: null